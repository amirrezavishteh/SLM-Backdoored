# Visual Workflow Diagrams

## 🔄 Complete Pipeline Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                    LOOKBACK-LENS DETECTION PIPELINE                  │
│                   For SLMs: Gemma 2-2B, Granite 3.1-3B               │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│  STEP 0: AUDIT   │  ⭐ START HERE - CRITICAL!
│  (Visualization) │
└────────┬─────────┘
         │
         ├─────► Verify trigger tokenization
         │       └─ Test-Trigger
         │
         ├─────► Generate clean & triggered outputs
         │       └─ Model forward pass with output_attentions=True
         │
         ├─────► Extract attention maps
         │       └─ [num_steps, num_layers, 1, heads, 1, seq]
         │
         ├─────► Compute ratios per head/layer/step
         │       ├─ Backdoor: TR = A(trigger) / (A(trigger) + A(non-trigger))
         │       └─ Hallucination: LR = A(context) / (A(context) + A(new))
         │
         ├─────► Create visualizations
         │       ├─ Heatmaps: attention weights over generation
         │       ├─ Ratio curves: TR or LR over time
         │       └─ Layer×head grids: average ratios
         │
         └─────► Verify signal exists ✓
                 ├─ YES → Proceed to feature extraction
                 └─ NO  → Adjust trigger/prompts/model

┌──────────────────┐
│ STEP 1: FEATURES │
│   (Extraction)   │
└────────┬─────────┘
         │
         ├─────► Prepare labeled dataset
         │       ├─ Backdoor: {"prompt": "...", "label": 0/1}
         │       └─ Hallucination: {"prompt": "...", "span_labels": [1,0,1,...]}
         │
         ├─────► For each example:
         │       ├─ Generate with attentions
         │       ├─ Compute ratios per head/layer/step
         │       ├─ Aggregate over chunks (default: 8 tokens)
         │       └─ Concatenate: [num_layers × num_heads] feature vector
         │
         └─────► Save features.npz
                 ├─ X: [n_samples, num_features]
                 └─ y: [n_samples] binary labels

┌──────────────────┐
│ STEP 2: TRAINING │
│   (Detection)    │
└────────┬─────────┘
         │
         ├─────► Load features (train & test)
         │
         ├─────► Train logistic regression
         │       └─ clf = LogisticRegression().fit(X_train, y_train)
         │
         ├─────► Evaluate on test set
         │       ├─ AUROC (>0.9 is excellent)
         │       ├─ FPR@95%TPR (<0.1 is excellent)
         │       └─ Accuracy
         │
         └─────► Save detector.pkl ✓

┌──────────────────┐
│ STEP 3: DEPLOY   │
│  (Production)    │
└────────┬─────────┘
         │
         └─────► Use trained detector on new inputs
                 ├─ Extract features from new example
                 ├─ clf.predict_proba(features)
                 └─ Flag if score > threshold
```

---

## 🔍 Backdoor Detection Workflow

```
INPUT: User prompt (with or without trigger)
   │
   ▼
┌──────────────────────────────────────┐
│  Generate response with attentions   │
│  max_new_tokens=64, do_sample=True   │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Extract attention per step          │
│  Shape: [steps][layers][heads][seq]  │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Identify trigger token indices      │
│  tokenizer.encode(trigger_text)      │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────────┐
│  Compute Trigger Ratio (TR) per head/layer/step  │
│                                                   │
│  For each (layer l, head h, step t):             │
│    A_trigger = Σ attn[trigger_indices]           │
│    A_other   = Σ attn[non_trigger_indices]       │
│    TR^{l,h}_t = A_trigger / (A_trigger + A_other)│
└──────────────┬───────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Aggregate over chunks (8 tokens)    │
│  TR_chunk = mean(TR_t) over chunk    │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Concatenate into feature vector     │
│  [TR_L0_H0, TR_L0_H1, ..., TR_Ln_Hm] │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Logistic regression predicts:       │
│  y = 0 (clean) or 1 (backdoor)       │
└──────────────────────────────────────┘
```

---

## 💭 Hallucination Detection Workflow

```
INPUT: Question + Context document
   │
   ▼
┌──────────────────────────────────────┐
│  Generate response with attentions   │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Extract attention per step          │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Identify context token indices      │
│  (original prompt before generation)  │
└──────────────┬───────────────────────┘
               │
               ▼
┌────────────────────────────────────────────────────┐
│  Compute Lookback Ratio (LR) per head/layer/step   │
│                                                     │
│  For each (layer l, head h, step t):               │
│    A_context = Σ attn[context_indices]             │
│    A_new     = Σ attn[new_generated_indices]       │
│    LR^{l,h}_t = A_context / (A_context + A_new)    │
└──────────────┬─────────────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Aggregate over spans (8 tokens)     │
│  LR_span = mean(LR_t) over span      │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Concatenate into feature vector     │
│  [LR_L0_H0, LR_L0_H1, ..., LR_Ln_Hm] │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│  Logistic regression predicts:       │
│  y = 1 (factual) or 0 (hallucinated) │
└──────────────────────────────────────┘
```

---

## 🎨 Visualization Output Structure

```
outputs/audit_backdoor_gemma/
│
├── heatmaps/
│   ├── example_1_triggered/
│   │   ├── example_1_triggered_L0_H0.png    ◄── Early layer, early head
│   │   ├── example_1_triggered_L13_H8.png   ◄── Mid layer, mid head
│   │   └── example_1_triggered_L25_H15.png  ◄── Late layer, late head
│   └── example_2_triggered/
│       └── ...
│
├── ratio_curves/
│   ├── example_1_TR_curve.png     ◄── Trigger ratio over generation steps
│   ├── example_2_TR_curve.png
│   └── ...
│
├── layer_head_grids/
│   ├── example_1_triggered_grid.png  ◄── Heatmap: layers × heads
│   └── ...
│
├── separation_scores/
│   ├── example_1_separation.npy      ◄── Numerical scores per head
│   └── ...
│
└── audit_summary.txt                 ◄── Text summary, top heads
```

---

## 📊 Data Flow

```
┌──────────────┐
│ Raw Prompts  │
└──────┬───────┘
       │
       ▼
┌────────────────────────────┐
│ Model Generation           │
│ + Attention Extraction     │
└──────┬─────────────────────┘
       │
       ├─────────────────────┐
       │                     │
       ▼                     ▼
┌──────────────┐      ┌──────────────┐
│ Backdoor     │      │ Hallucination│
│ Mode         │      │ Mode         │
└──────┬───────┘      └──────┬───────┘
       │                     │
       ▼                     ▼
┌──────────────┐      ┌──────────────┐
│ Trigger      │      │ Lookback     │
│ Ratios (TR)  │      │ Ratios (LR)  │
└──────┬───────┘      └──────┬───────┘
       │                     │
       └──────────┬──────────┘
                  │
                  ▼
           ┌──────────────┐
           │ Feature      │
           │ Vectors      │
           └──────┬───────┘
                  │
                  ▼
           ┌──────────────┐
           │ Logistic     │
           │ Regression   │
           └──────┬───────┘
                  │
                  ▼
           ┌──────────────┐
           │ Predictions  │
           │ (0 or 1)     │
           └──────────────┘
```

---

## 🔄 CLI Command Flow

```
┌─────────────────────┐
│  cli_helper.ps1     │  Load PowerShell helpers
└─────────┬───────────┘
          │
          ▼
    ┌──────────────────────────────────────┐
    │         QUICK FUNCTIONS              │
    ├──────────────────────────────────────┤
    │ Test-Trigger                         │
    │ Audit-Backdoor                       │
    │ Audit-Hallucination                  │
    │ Extract-BackdoorFeatures             │
    │ Extract-HallucinationFeatures        │
    │ Train-Detector                       │
    │ Run-BackdoorPipeline         (full)  │
    │ Run-HallucinationPipeline    (full)  │
    └─────────┬────────────────────────────┘
              │
              ▼
        ┌─────────────┐
        │  src/cli.py │  Python CLI
        └─────┬───────┘
              │
    ┌─────────┴─────────────┐
    │                       │
    ▼                       ▼
┌───────────┐        ┌──────────────┐
│ audit-attn│        │extract-features│
└─────┬─────┘        └──────┬────────┘
      │                     │
      ▼                     ▼
┌─────────────────┐   ┌─────────────────┐
│ visualization/  │   │ extraction/     │
│ audit_attention │   │ backdoor_       │
│                 │   │ features        │
└─────────────────┘   └─────┬───────────┘
                            │
                            ▼
                      ┌─────────────┐
                      │ features.npz│
                      └─────┬───────┘
                            │
                            ▼
                      ┌──────────────┐
                      │train-detector│
                      └──────┬───────┘
                            │
                            ▼
                      ┌─────────────┐
                      │ detection/  │
                      │ train_      │
                      │ detector    │
                      └─────┬───────┘
                            │
                            ▼
                      ┌─────────────┐
                      │detector.pkl │
                      └─────────────┘
```

---

## 🎯 Decision Tree: Which Command to Run?

```
                    START HERE
                        │
                        ▼
              ┌──────────────────┐
              │ Have you run     │
              │ audit yet?       │
              └────┬─────────┬───┘
                   │         │
                  NO        YES
                   │         │
                   ▼         ▼
         ┌─────────────┐  ┌──────────────┐
         │ Test-Trigger│  │ Did you see  │
         │     +       │  │ clear signal?│
         │ Audit-Xxx   │  └──┬────────┬──┘
         └─────────────┘     │        │
                            NO       YES
                             │        │
                             ▼        ▼
                    ┌────────────┐  ┌──────────────┐
                    │ Adjust:    │  │ Have labeled │
                    │ - trigger  │  │ dataset?     │
                    │ - prompts  │  └──┬────────┬──┘
                    │ - model    │     │        │
                    └────────────┘    NO       YES
                                      │        │
                                      ▼        ▼
                             ┌──────────────┐  ┌──────────────┐
                             │ Create data: │  │ Extract      │
                             │ .jsonl files │  │ Features     │
                             └──────────────┘  └──────┬───────┘
                                                      │
                                                      ▼
                                               ┌──────────────┐
                                               │ Train        │
                                               │ Detector     │
                                               └──────┬───────┘
                                                      │
                                                      ▼
                                               ┌──────────────┐
                                               │ Evaluate &   │
                                               │ Deploy ✓     │
                                               └──────────────┘
```

---

## 🏗️ Architecture Diagram

```
┌─────────────────────────────────────────────────────────┐
│                    USER INTERFACE                        │
├─────────────────────────────────────────────────────────┤
│  PowerShell Helpers  │  Direct CLI  │  Python Scripts   │
│  (cli_helper.ps1)    │  (src/cli.py)│                   │
└─────────────┬───────────────────────────────────────────┘
              │
┌─────────────┴───────────────────────────────────────────┐
│                   CORE MODULES                           │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────────┐  ┌──────────────┐  ┌───────────────┐  │
│  │  utils/     │  │ extraction/  │  │visualization/ │  │
│  │  - model    │  │ - attention  │  │ - heatmaps    │  │
│  │  - tokenizer│  │ - backdoor   │  │ - curves      │  │
│  └─────────────┘  │ - lookback   │  │ - audit       │  │
│                   └──────────────┘  └───────────────┘  │
│                                                          │
│  ┌─────────────┐  ┌──────────────┐                      │
│  │ detection/  │  │  training/   │                      │
│  │ - logreg    │  │ (placeholder)│                      │
│  └─────────────┘  └──────────────┘                      │
└──────────────────────────────────────────────────────────┘
              │
┌─────────────┴───────────────────────────────────────────┐
│                  EXTERNAL DEPENDENCIES                   │
├──────────────────────────────────────────────────────────┤
│  HuggingFace     PEFT          PyTorch      Scikit-learn│
│  Transformers    (LoRA)        (tensors)    (logreg)    │
│  matplotlib      seaborn       numpy                    │
└──────────────────────────────────────────────────────────┘
```

---

**For interactive use, start with**: [INDEX.md](INDEX.md) or [QUICKSTART.md](QUICKSTART.md)
